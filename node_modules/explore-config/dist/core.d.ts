export default function exploreConfig(source: string | string[], options?: {
    cwd?: string;
    keys?: string[];
    on?: {
        found?: (foundPath: string) => void;
        notFound?: () => void;
    };
}): any;
