import { Config } from '../config';
/**
 * Generates the sorted CSS text from HTML
 * @param html
 * @param config
 * @returns joined css text
 */
export default function generateFromHTML(html: string, config?: Config): string;
