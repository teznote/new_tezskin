import { Config } from '../config';
/**
 * Generates the sorted CSS text from classes
 * @param classes
 * @param config
 * @returns joined css text
 */
export default function generateFromClasses(classes: string[], config?: Config): string;
