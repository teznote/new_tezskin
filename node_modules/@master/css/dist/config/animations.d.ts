import type { CSSProperties } from '../types/css-properties';
declare const animations: Animations;
export default animations;
export type Animations = Record<string, {
    [key in 'from' | 'to']?: CSSProperties;
} & {
    [key: string]: CSSProperties;
}>;
