import type { CSSProperties } from '../types/css-properties';
declare const semantics: Semantics;
export default semantics;
type SemanticsBase = CSSProperties | {
    [key: string]: SemanticsBase;
};
export type Semantics = {
    [key: string]: SemanticsBase;
};
